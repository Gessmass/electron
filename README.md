# electron
Electron Test bluetooth connections

Run dev mode :
    /app => npm run start
    / => npm run start:dev

Run build mode :
(check first if mainWindow.loadURL is ok in main.js)
    / => npm run start:dev